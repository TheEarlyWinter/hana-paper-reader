# Hana Paper Reader 0.9.0 实机验证说明

> 目的：在真实 HanaAgent / Chromium WebView 中验证 PDF 导入、论文库、论文切换、元数据与正文状态保存、导出下载、MinerU 网络链路以及宿主会话桥接。
>
> 本说明只指导验证，不修改现有论文数据。优先使用项目 fixture；除非测试项明确要求，不要删除用户已有论文。
>
> 项目目录：`C:\Users\WIT_User\Desktop\OH-WorkSpace\hana-paper-reader`
>
> 开发插件源码：`C:\Users\WIT_User\Desktop\OH-WorkSpace\hana-paper-reader`
>
> 当前版本：`0.9.0`

---

## 1. 验证前准备

### 1.1 建议环境

- HanaAgent 已启动。
- 使用当前工作区加载的开发插件，不要误测旧 ZIP：
  - 插件 ID：`hana-paper-reader`
  - 版本：`0.9.0`
- 打开 Hana 的插件开发诊断，确认插件已加载且无激活错误。
- 准备项目 fixture：

```text
tests\fixtures\regression\english-two-column.pdf
tests\fixtures\regression\chinese-paper.pdf
tests\fixtures\regression\figure-table-dense.pdf
tests\fixtures\regression\formula-dense.pdf
tests\fixtures\regression\scanned-paper.pdf
```

- 建议在 Windows `Downloads` 目录测试导出前记下已有文件，避免把旧文件误认为本次产物。
- 不要把真实 MinerU Token、模型凭据或私人论文复制进仓库、截图或 QA 文档。

### 1.2 数据保护规则

- 优先点击「示例论文」或导入 fixture。
- 不要对用户已有论文执行「删除整篇论文及其研究数据」或不可逆清理。
- 如果必须测试删除、备份恢复或清理，先使用一篇 fixture，并先导出完整备份。
- 测试过程中不要执行 `git reset`、`git clean`、回滚或覆盖工作区文件。
- 每个异常先截图和记录步骤，再刷新或重载插件；不要先清空数据“自救”。

### 1.3 记录模板

每个用例至少记录：

```text
用例 ID：
执行时间：
插件版本 / dev run：
输入 fixture：
操作步骤：
实际结果：
预期结果：
结论：通过 / 失败 / 阻塞 / 环境问题
证据：截图文件名、UI 文案、下载文件名、错误信息
```

判定规则：

- **通过**：实际结果满足全部预期，且刷新/切换后仍成立。
- **失败**：出现数据丢失、论文串数据、错误成功提示、下载内容错误、旧请求覆盖新状态或页面崩溃。
- **阻塞**：缺 Token、网络不可用、宿主能力缺失等导致无法执行；必须写清阻塞原因，不要记为通过。
- **环境问题**：异常发生在文件选择器、系统权限或宿主窗口操作层，且插件请求未发出；保留证据并单独标记。

---

## 2. 当前基线

截至本说明生成前，已在真实 HanaAgent 中确认：

- 通过地址栏定位成功导入 `english-two-column.pdf`。
- 双栏正文、标题、Abstract、段落顺序正常显示。
- 鼠标滚轮和 `PageDown` 可以滚动正文。
- 论文库出现 `english-two-column.pdf`，库内显示多篇论文。
- 返回论文库后重新打开该论文，正文可以恢复，未观察到数据丢失。

已知但尚未归因于 HPR 代码的问题：

- 一次系统文件选择器中路径输入被拆分或进入错误字段，弹出“找不到文件”。改用文件选择器地址栏后导入成功。后续若复现，先记录为文件选择器/操作层异常，不要直接修改 HPR 导入代码。

下列项目需要继续执行：

1. 元数据与正文编辑保存、自动保存和 revision。
2. 多论文快速切换的状态隔离。
3. Markdown、CSV、完整备份导出及宿主下载桥接。
4. MinerU Token、真实上传/轮询/结果下载链路。
5. Agent、聊天模型、已有对话和新建对话桥接。
6. 多 WebView / 多卡片并发及迟到响应。

---

## 3. P0/P1 数据正确性验证

### HPR-M01：同一论文的元数据修改可保存并恢复

**前置条件**：已导入或打开 `english-two-column.pdf`。

**步骤**：

1. 在论文库确认该论文卡片存在。
2. 测试收藏按钮：点击星标一次，等待成功反馈，再回到库或刷新库。
3. 再次打开论文，回到库确认收藏状态保持。
4. 测试归档按钮：点击归档，再筛选“已归档”；确认论文只出现在归档列表。
5. 取消归档，确认回到活动论文列表。
6. 如果当前 UI 提供标题、作者、年份、DOI、标签编辑入口，逐项修改为明显的 fixture 值，例如：
   - 标题：`QA Fixture Paper 0.9.0`
   - 作者：`QA Author A; QA Author B`
   - 年份：`2026`
   - 标签：`qa, fixture`
7. 重新打开论文、切换到库，再刷新页面。

**预期**：

- 收藏和归档状态正确保存，不影响其他论文。
- 元数据字段按输入规范化：首尾空格清理；作者、标签不应出现对象字符串或空项目；非法/过长值应有明确拒绝或清洗提示。
- 标题在阅读器顶部、论文标签页和论文库显示一致。
- 刷新后修改仍存在。
- 其他论文的标题、作者、标签、收藏和归档状态不变。

**失败重点**：成功提示但刷新丢失；修改 A 后 B 变化；标题显示更新但库排序/搜索仍用旧值；服务端 revision 不递增或倒退。

---

### HPR-M02：正文/译文/用户定稿自动保存，且用户定稿不会被覆盖

**前置条件**：打开有可翻译段落的论文；如果无模型，先用已有缓存或记录阻塞，不要伪造通过。

**步骤**：

1. 记录当前论文标题、首段正文、当前阅读模式和页面位置。
2. 选择阅读模式：`原文`、`双语`、`译文`、`对照`，每种模式至少停留一次。
3. 在 `双语` 或 `译文` 模式点击一个段落的 `译`。
4. 等待译文显示，确认标识为 `AI 译文`。
5. 点击 `定稿`，将译文改成明显的测试文本，例如：`QA USER FINAL — DO NOT OVERWRITE`，点击 `保存为定稿`。
6. 等待成功提示；切换到其他阅读模式再切回。
7. 触发一次正文滚动或切换论文，让自动保存有机会完成；等待至少 1 秒。
8. 返回该论文，刷新插件或关闭并重新打开论文标签页。
9. 如可操作，修改同一段原文关联状态/阅读位置，再等待自动保存。
10. 再次执行该段 `重新翻译全文` 或术语修改（若模型/工作流可用）。

**预期**：

- AI 翻译成功后显示 `AI 译文`。
- 用户定稿成功后显示 `用户定稿` / `已定稿`，内容准确保留。
- 切换阅读模式不丢失正文、译文和定稿。
- 刷新、返回库、重新打开后内容仍存在。
- 术语版本变化只使受影响的 AI 译文进入待重译状态；用户定稿不被全文翻译或术语更新覆盖。
- 自动保存失败时必须有可见错误或重试状态，不能静默显示“已保存”。
- revision 只能保持不变或递增，不能因迟到响应回退。

**失败重点**：定稿被重新翻译覆盖；刷新后丢失；保存成功提示与服务端内容不一致；revision 倒退；切换论文后迟到翻译写入另一篇论文。

---

### HPR-M03：阅读进度和滚动位置按论文隔离

**步骤**：

1. 打开论文 A：`english-two-column.pdf`。
2. 将原文区滚动到中后部，记录可见标题；如果有阅读进度，记录百分比和页码。
3. 切换到论文库，再打开论文 B：`chinese-paper.pdf` 或另一篇已有 fixture。
4. 将 B 滚动到不同位置，切换一种不同阅读模式。
5. 在 A/B 之间连续快速切换至少 5 次；其中至少一次在页面尚未完全加载时立即切换。
6. 关闭 A 的标签页但不要删除论文；重新从库打开 A。
7. 关闭 B 的标签页并重新打开 B。

**预期**：

- A 的正文、译文、标题、阅读模式、活动段落、搜索/笔记草稿和滚动位置只属于 A。
- B 不显示 A 的正文、译文、笔记、标题或进度。
- 快速切换后，迟到的加载、翻译、进度或研究工具响应不能改写当前论文。
- 关闭标签页只关闭视图，不删除论文数据。
- 重新打开标签页后各自数据完整。

**失败重点**：A 的正文出现在 B；A 的滚动位置覆盖 B；旧论文标题和新正文混合；切换后研究抽屉仍显示上一论文；标签页关闭导致论文库数据消失。

---

## 4. 导出与下载验证

### HPR-M04：双语 Markdown 直接落盘

**前置条件**：打开一篇 fixture 论文；最好至少有一段 AI 译文、用户定稿、笔记或书签。

**步骤**：

1. 点击 `研究工作流`。
2. 进入 `沉淀` → `研究导出`。
3. 点击 `导出双语 Markdown`。
4. 等待通知，检查 Windows `Downloads` 目录。
5. 打开最新生成的 `.md` 文件，使用 UTF-8 读取。
6. 再次点击导出，确认同名文件处理方式。
7. 如果标题含 Windows 非法字符（`\\ / : * ? " < > |`），用有此类字符的元数据测试一次；不要覆盖原论文。

**预期**：

- 成功提示明确说明保存到下载文件夹，并给出实际文件名。
- 文件确实存在、可读，编码为 UTF-8。
- 文件包含：论文标题、原文结构块、`Page X / block Y` 锚点、AI 译文/用户定稿状态、可用笔记/书签/进度/术语。
- Markdown 中的公式、链接、列表和特殊字符不被错误转义；用户内容不会被当作 HTML 执行。
- 同名导出不覆盖旧文件，应生成 `(1)`、`(2)` 等后缀或明确报错。
- 非法文件名字符被清洗，路径不会逃逸到 Downloads 之外。

**失败重点**：只弹出下载但 Downloads 没文件；文件为空或编码乱码；导出的是另一篇论文；标题/笔记丢失；文件被覆盖；出现绝对路径或路径穿越。

---

### HPR-M05：完整备份可导出、可恢复且不包含敏感配置

**步骤**：

1. 进入 `研究工作流` → `数据与任务`。
2. 先点击 `导出完整备份`。
3. 检查 Downloads 中生成的 `hana-paper-reader-<hash前缀>.backup.json`。
4. 用文本编辑器或 PowerShell 查看 JSON 结构；不要把完整文件上传或粘贴到聊天。
5. 确认内容包含论文结构、翻译状态、笔记、书签、进度、术语和任务（若存在）。
6. 搜索备份文本中的以下敏感关键词和值：`Bearer`、`mineruApiToken`、Token 实际值、宿主 session path、模型凭据。
7. 在 fixture 论文上修改一个可识别字段，执行 `从备份恢复`。
8. 重新打开论文并核对恢复结果。
9. 如需验证错误恢复，复制一份备份并只在副本上改动：将 `paperHash` 改成另一篇论文的 hash，或改坏 JSON；确认恢复被拒绝且当前论文未被破坏。

**预期**：

- 备份文件真实存在且 JSON 可解析。
- 备份不含 MinerU Token、模型会话凭据或宿主敏感路径。
- 正确备份可以恢复同一论文的研究数据。
- 错论文备份、损坏 JSON、非法附件路径、超限附件应明确失败；失败前不应部分替换当前数据。
- 其他论文数据不受恢复影响。

**失败重点**：恢复错论文却成功；恢复失败后部分数据被替换；备份含 Token/凭据；恢复后正文/定稿/笔记缺失。

---

### HPR-M06：表格 CSV 与视觉证据导出

**前置条件**：导入 `figure-table-dense.pdf`；如 MinerU 不可用，记录阻塞，不要将没有真实视觉资源记为通过。

**步骤**：

1. 打开 `研究工作流` → `核验` → `图表、表格与公式`。
2. 筛选图、表、公式，逐个检查 `回到正文`。
3. 对表格点击 `导出 CSV`。
4. 用 UTF-8 兼容表格工具打开 CSV。
5. 点击 `复制带来源 Markdown`，粘贴到纯文本编辑器。
6. 对公式点击复制 LaTeX（若有）。

**预期**：

- 每个视觉对象能回到正确论文、页码和 block。
- 表格 CSV 含表头/数据，中文不乱码，CSV 字段中的逗号、引号和换行按 CSV 规则转义。
- Markdown 含正确的 `Page`、`block`、`Evidence` 信息。
- 用户提供的表格 HTML 只作为代码块/安全渲染内容，不执行事件属性或脚本。
- 当前论文的视觉资源不会串到另一篇论文。

---

## 5. MinerU 真实网络链路

### HPR-M07：MinerU 设置保存与 Token 不回显

**步骤**：

1. 点击顶部 `MinerU 未配置` 或 `MinerU · VLM`。
2. 填入有效 Token；选择模型 `VLM` 或 `Pipeline`，语言、公式、表格和 OCR 参数。
3. 点击 `保存设置`。
4. 关闭设置，再次打开；不输入新 Token，只修改一个非 Token 设置并保存。
5. 观察顶部状态和错误提示。
6. 如 Token 无效，使用最小 fixture 发起一次导入，记录失败信息。

**预期**：

- Token 输入框是密码类型，保存后不回显实际 Token。
- 留空 Token 后保存非敏感设置不会清空原 Token（如果 UI 文案声明“留空保持原值”）。
- 模型、语言、公式、表格、OCR 设置按选择持久化。
- Token 无效时显示可理解的认证/配置错误，不泄露 Token。
- 日志、错误提示、导出备份和前端 DOM 不出现完整 Token。

**失败重点**：Token 明文显示；保存后丢失；错误消息带出 Token；配置状态与真实请求不一致。

---

### HPR-M08：MinerU 上传、轮询、结果下载与 OCR fallback

**前置条件**：有效 MinerU Token、网络可用；先用 `english-two-column.pdf`，再按需使用 `scanned-paper.pdf`。

**步骤**：

1. 导入 `english-two-column.pdf`，记录从上传到完成的每个阶段文案。
2. 观察是否出现重复上传、无限加载或阶段倒退。
3. 完成后核对标题、段落、页码、图片/表格/公式资源。
4. 再次导入相同 PDF，确认命中 hash/解析缓存，不应重复完整解析（除非点击 `MinerU 重解析`）。
5. 导入 `scanned-paper.pdf`，观察普通解析失败后是否触发一次 OCR fallback。
6. 若任务失败、超时、401/403、上传地址失败或结果 ZIP 无结构，记录最终错误和是否错误地重复 OCR。
7. 如网络条件允许，打开 `数据与任务`，观察任务状态、阶段、失败原因和取消按钮。

**预期**：

- PDF 请求按当前版本走原始二进制上传；上传、轮询、结果 ZIP 下载完成后展示明确阶段。
- 网络失败、401/403、Token 缺失、超时都有明确错误；不存在死循环或无限重试。
- 普通解析明确失败且结果无可用结构时最多触发一次 OCR fallback；已经是强制 OCR 时不重复 OCR。
- 相同 PDF 通过 SHA-256 命中缓存，不重复消耗 MinerU 任务；强制重解析才重新提交。
- 解析结果中的正文顺序、标题、图表、表格、公式和页码可核对；无关资源不会被错误缓存。
- 任务取消后不会继续把迟到结果写入当前论文。

**失败重点**：上传 JSON/Base64 协议错误；批量接口参数位置错误；无限轮询；普通错误盲目 OCR；同一文件每次都重复解析；结果跨论文写入；失败提示泄露 Token。

**无 Token / 无网络时的记录方式**：标记 `阻塞`，记录“未执行真实 MinerU 链路”，不得写成“解析通过”。

---

## 6. Agent、模型与宿主桥接

### HPR-M09：动态 Agent 与聊天模型列表

**步骤**：

1. 在阅读器中打开需要翻译或解释的功能。
2. 观察 Agent 选择器是否读取当前 Hana 中可用的 Agent。
3. 观察聊天模型列表是否为 `provider/id` 精确引用，而不是只有显示名。
4. 选择一个可用模型，执行单段翻译或概念解析。
5. 切换 Agent/模型，再执行另一段操作。
6. 如果模型列表读取失败或宿主版本不支持，记录状态码、提示和回退行为。

**预期**：

- 已删除/不可用 Agent 不出现在选择器。
- 模型显示可读名称，但实际选择保持精确 `provider/id`。
- 切换 Agent/模型后新请求使用新上下文；旧翻译缓存不被错误复用。
- 模型目录不可用时有明确提示，不自动创建不可见会话。

---

### HPR-M10：发送到已有对话与显式新建对话

**前置条件**：Hana 中至少有一个可见已有对话；若没有，只执行“无目标”错误路径。

**步骤**：

1. 在正文中选中一段原文，点击 `发送到对话…`。
2. 检查对话列表，确认只展示可选择的已有对话，不暴露宿主 session path、内部 ID 或凭据。
3. 选择已有对话，点击 `发送到所选对话`。
4. 到目标对话中核对论文标题、原文引用、页码/block 证据和上下文。
5. 再从论文中点击发送，选择 `新建对话并发送`。
6. 核对只创建一次会话，消息只发送一次。
7. 关闭选择框、快速切换论文，再观察迟到的列表/发送响应。

**预期**：

- 无显式目标时不能静默创建会话或发送消息。
- 已有对话发送成功，目标正确，内容只发送一次。
- 新建对话只在用户明确点击后发生，模型选择按用户选择执行。
- 目标列表不泄露宿主路径、session ID 等内部信息。
- 发送失败时提示失败，不显示成功，不重复发送。
- 切换论文后旧选择器响应不能更新新论文状态。

**失败重点**：点击已有对话却新建会话；无目标自动建会话；发到错误对话；重复发送；泄露 session path；旧论文引用发送到新论文。

---

## 7. 多 WebView / 并发一致性验证

这是高风险场景，建议在不涉及用户原始论文的 fixture 上执行。

### HPR-M11：两个论文阅读器实例同时操作

**准备**：通过 Hana 打开两个 HPR 卡片/页面实例；如果宿主不支持，标记阻塞并记录宿主限制。

**步骤**：

1. 实例 1 打开论文 A，实例 2 打开论文 A。
2. 实例 1 修改阅读模式/进度并等待自动保存。
3. 实例 2 修改另一项状态并等待自动保存。
4. 两实例快速交替修改，观察论文库和重新打开结果。
5. 实例 1 删除 fixture 论文 A；实例 2 在删除请求前后触发滚动、自动保存、翻译缓存写入或进度保存。
6. 等待所有网络请求结束，再刷新论文库。

**预期**：

- 同一论文的更新不会被旧实例静默覆盖；出现 revision 冲突时应重读/提示，而不是假成功。
- 删除后迟到的自动保存、进度、翻译缓存不能让论文重新出现在库中。
- 删除操作完成后两个实例都能退出该论文或显示不存在，不继续写入。
- 其他论文数据不受影响。

**失败重点**：删除论文复活；旧状态覆盖新状态；实例 2 仍显示成功但服务器内容已被实例 1 覆盖；数据结构损坏。

---

## 8. 失败与边界用例

### HPR-M12：文件选择器与无效输入

依次测试以下输入并记录发生在哪一层：

- 取消系统文件选择器。
- 选择不存在的路径。
- 选择非 PDF 的图片、`.exe` 或超大文件。
- 选择空 TXT/Markdown。
- 选择损坏 PDF。
- 输入带空格、中文、括号和多个点号的文件名。
- 选择同一 PDF 两次。

**预期**：

- 取消不改变当前论文和库数据。
- 不支持格式、空文件、损坏 PDF 和超限文件有明确提示。
- 无效文件不生成空论文、不覆盖已有论文、不创建悬挂任务。
- 文件名显示正确，不因空格/中文/括号截断或错误转义。
- 同一 PDF 命中同一 hash 和缓存。

### HPR-M13：刷新、关闭、重新打开

1. 在论文 A 中滚动、切换阅读模式、留下笔记草稿（若支持），等待自动保存。
2. 刷新 WebView 或关闭并重新打开 HPR。
3. 检查最近论文、正文、进度、阅读模式、笔记、译文和搜索状态。
4. 对 PDF 原页预览单独记录：结构内容应恢复；原页 PDF 句柄可能需要重新选择同一 PDF，这是产品文档声明的限制。

**预期**：结构研究数据恢复；原页预览若要求重新选 PDF，应有明确提示且重新选择后命中缓存，不重复解析。

---

## 9. 建议的执行顺序

按下列顺序执行，出现 P0/P1 时先停止扩展测试并保留证据：

1. HPR-M01 元数据保存。
2. HPR-M02 正文/译文/定稿保存。
3. HPR-M03 多论文状态隔离。
4. HPR-M04 Markdown 导出。
5. HPR-M05 完整备份恢复。
6. HPR-M06 视觉对象/CSV/LaTeX。
7. HPR-M07 MinerU 设置。
8. HPR-M08 MinerU 网络链路。
9. HPR-M09 Agent/模型。
10. HPR-M10 宿主会话桥接。
11. HPR-M11 多 WebView 并发。
12. HPR-M12/HPR-M13 边界和恢复。

---

## 10. 缺陷报告最小格式

```markdown
## [P1] HPR-xxx：简短标题

- 环境：HanaAgent 版本 / Windows / 插件版本 / dev run
- 输入：fixture 文件名、论文 hash 前缀（不要写 Token）
- 前置条件：
- 重现步骤：
  1. ...
  2. ...
- 预期：
- 实际：
- 频率：必现 / 偶现 / 一次
- 影响：数据丢失 / 串论文 / 下载失败 / UI 错误 / 环境阻塞
- 证据：截图、错误文案、下载文件名；敏感信息已打码
- 是否可由 Node 测试复现：是 / 否 / 未尝试
```

严重度建议：

- P0：数据丢失、跨论文泄露、凭据泄露、无法启动。
- P1：核心导入/保存/切换/导出在常见路径失败，或数据错误。
- P2：并发、兼容性、资源泄漏、异常恢复或明显 UX 问题。
- P3：文案、可维护性、低风险边界和测试覆盖问题。

---

## 11. 0.9.0 本轮验证结论（2026-08-30）

- 环境：Windows 10.0.19045 / HanaAgent 0.769.0。
- 运行版本：`dev:hana-paper-reader`，dev run：`dev_1788098913777_3b8f0ad50af0`。
- 运行时确认：开发版已加载并 shadow 正式版；资源缓存版本为 `0.9.0-r1`。
- 数据保护：未删除现有论文，未删除 Downloads 历史文件，未写入或暴露 MinerU Token。

### 已通过

- [x] 卡片资源加载完成，未停留在 loading 壳。
- [x] 文库删除按钮响应正常，确认弹窗可交互；按约定仅验证取消路径，未执行最终删除。
- [x] CSV 导出按钮响应正常，导出提示和文件落盘路径正常。
- [x] “复制带来源 Markdown”响应正常。
- [x] 浏览器拒绝剪贴板写入时，复制降级路径可用或给出明确反馈。
- [x] 公式对象显示“复制 LaTeX”入口并可操作。
- [x] 既有论文数据未被本轮测试删除或覆盖。

### 自动门禁

- [x] 全量 Node 测试：75/75 通过。
- [x] JavaScript / MJS 语法检查通过。
- [x] `git diff --check` 无空白错误。
- [x] 资源会话参数保留回归通过；跨源 `hana-asset-base` 被拒绝。

### 仍未宣称完成

- [ ] 真实 MinerU Token 上传、轮询、OCR fallback。
- [ ] 真实翻译模型与宿主会话桥接。
- [ ] 多 WebView 并发实机压测。
- [ ] 系统文件选择器异常路径与完整重启恢复。

这些项目不阻塞本版已修复 UI 交互的发布，但不能在发布说明中表述为已完成验收。

---

## 12. 验证结束时的结论模板

```text
实机验证范围：HPR-M01 ~ HPR-M13
通过：
失败：
阻塞：
环境问题：
已确认缺陷：
未能确认的风险：
证据目录：
是否建议发布：是 / 否 / 需修复后复测
```

注意：Node 自动测试、语法检查和打包通过，只能证明离线门禁通过，不能替代本说明中的真实 HanaAgent / Chromium / Windows Downloads / MinerU / 宿主 session 验证。
