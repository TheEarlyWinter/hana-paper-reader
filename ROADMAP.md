# Hana Paper Reader 产品路线图

> 当前版本：0.5.0
> 文档定位：记录产品方向、功能优先级和实现边界，不等同于已经承诺的发布日期。

## 一、产品定位

Hana Paper Reader 不应继续扩展成一个普通的 PDF 阅读器。它的核心价值是把论文变成一个**可追溯、可交互、能辅助研究的证据工作台**：

- MinerU 负责提取论文的语义结构；
- PDF.js 保留原始页面和视觉证据；
- 连续双语阅读流提升阅读效率；
- Hana 助手负责解释、翻译和追问；
- 页码、坐标和结构块让每个结论都能回到原文。

产品的长期目标不是只生成一份漂亮的摘要，而是让用户能够：

1. 快速读懂论文；
2. 随时定位原文证据；
3. 记录自己的判断和问题；
4. 将阅读结果沉淀为可复用的研究资料。

一句话概括：**0.4.2 解决“论文能不能稳定进来”，后续版本解决“进来以后能不能被可靠地理解、引用和沉淀”。**

## 二、当前基础

0.5.0 已经具备后续演进所需的基础能力：

- PDF 统一通过 MinerU 精准解析；
- 新卡片使用原始 `application/pdf` 二进制上传；
- 兼容升级后仍存活的旧 Base64 卡片请求；
- 普通解析失败时支持受控 OCR fallback；
- 连续单栏正文和中英文双侧阅读；
- 图片、图表、表格和公式结构块；
- PDF.js 原页预览和坐标定位裁剪；
- Hana 助手、头像、模型和思考档位；
- MinerU Token 服务端敏感配置；
- 响应式工具栏和窄屏上下分屏；
- 结构块页码、边界框和资源缓存。

这些能力意味着后续重点应放在**可引用性、可搜索性、可沉淀性**，而不是继续增加第二套 PDF 解析路线。

## 三、产品原则

### 3.1 证据优先

所有由助手生成的解释、总结和抽取结果，都应尽可能附带页码、结构块或原文片段。无法定位来源的内容必须明确标记为推断，而不是伪装成论文结论。

### 3.2 结构和视觉分离

MinerU 负责结构理解，PDF.js 负责原页展示和视觉定位。两者保持职责边界，不恢复本地 PDF 结构解析器。

### 3.3 低成本动作优先

搜索、目录、定位、笔记和缓存应优先在本地完成。模型调用和 MinerU 请求只在用户明确触发时执行。

### 3.4 结果可复用

阅读结果应能导出为 Markdown、研究笔记或结构化数据，并保留页码和证据锚点，而不是只能停留在当前卡片里。

### 3.5 隐私和可控网络

Token 只保存在服务端敏感配置中。高成本或外发操作需要明确的用户动作，解析缓存和笔记应有清晰的删除入口。

### 3.6 先做可解释的简单方案

在论文内搜索和结构块过滤足够解决问题时，不急于引入复杂向量数据库。先建立可靠的关键词、章节和选区检索，再根据实际使用数据决定是否增加语义检索。

## 四、功能路线

## 0.5：可引用精读

目标：让用户能够快速找到论文内容，并把每个阅读结论绑定回原文。

### 4.1 段落级引用锚点（最高优先级）

每个结构块继续维护稳定的引用信息：

```text
paperHash
blockId
page
bbox
blockType
```

计划功能：

- 点击译文跳回原文对应段落；
- 点击引用跳转到 PDF 原页并高亮边界框；
- 复制正文时可选择是否附带页码；
- 复制为 Markdown 引用；
- 生成包含来源的助手上下文；
- 在回答中展示“依据：第几页、第几个结构块”。

推荐引用格式：

```markdown
> [p. 5 | block: mineru_p5_b12]
> Original paragraph...

**中文译文：** ...
```

验收标准：任何来自论文正文的助手回答，都能至少定位到页码；用户点击来源后可以回到原文位置。

### 4.2 论文内搜索和大纲导航

计划功能：

- 全文关键词搜索；
- 原文和译文同时搜索；
- 搜索结果按页码、章节和结构块显示；
- 点击结果定位到双侧阅读区；
- 根据标题结构自动生成目录；
- 按图片、表格、公式、参考文献筛选；
- 显示当前命中的章节和页码。

示例结果：

```text
第 7 页 · Methods · 原文命中
第 7 页 · 方法 · 译文命中
```

第一版优先使用本地索引，不依赖模型或外部搜索服务。

### 4.3 锚定式笔记、书签和阅读进度

笔记绑定到论文结构块，而不是漂浮在论文之外。建议数据结构：

```text
paperHash
blockId
page
note
tags
createdAt
updatedAt
```

计划功能：

- 段落批注；
- 页面书签；
- 重点、高风险结论和待验证问题；
- 每篇论文的阅读进度；
- 记录已翻译和未翻译范围；
- 按标签筛选笔记；
- 重新解析同一文件后恢复批注；
- 删除单条笔记和清空整篇论文数据。

### 4.4 解析缓存和任务中心

MinerU 是在线服务，解析过程需要可观察、可恢复。

计划功能：

- 按 PDF SHA-256 复用解析结果；
- 同一文件不重复上传；
- 持久化任务状态；
- 显示申请地址、上传、轮询、下载和结构化阶段；
- 取消正在等待的任务；
- 从失败阶段继续，而不是无条件从头开始；
- 显示实际使用的模型和是否启用了 OCR fallback；
- 管理和删除本地解析缓存。

约束：缓存必须有大小上限、清理策略和用户可见的删除入口。

## 0.6：问论文和做笔记

目标：让 Hana 助手成为有证据边界的论文助手，而不是无来源的聊天窗口。

### 4.5 带证据的论文助手

支持四种上下文范围：

- 当前段落；
- 当前页面；
- 当前章节；
- 整篇论文。

计划功能：

- 询问当前选区；
- 解释术语和公式；
- 总结章节；
- 比较论文中的不同实验条件；
- 区分“论文原文说了什么”和“助手自己的推断”；
- 回答附带页码和结构块引用；
- 用户可以查看被注入助手上下文的原文片段。

第一版不必立即引入向量数据库。可以按以下顺序构造上下文：

1. 当前选区优先；
2. 标题和章节过滤；
3. 关键词命中；
4. 结构块类型筛选；
5. 将相关块注入当前助手回合。

### 4.6 术语表和翻译缓存

计划功能：

- 用户自定义术语表；
- 专有名词固定译法；
- 缩写和全称管理；
- 原文、当前译文和用户修订译文对照；
- 已翻译段落缓存；
- 只重新翻译被修改的段落；
- 统计术语在全文中的出现次数和位置。

对于化工、材料和能源论文，同一术语在全文中保持一致比单句翻译更重要。

### 4.7 图表、公式和图片实验室

将 MinerU 返回的视觉结构块做成独立视图：

- 图、表、公式分类浏览；
- 缩略图和标题列表；
- 点击跳回正文位置；
- 放大查看；
- 复制图题；
- 导出单张图片和对应说明；
- 表格复制为 Markdown 或 CSV；
- 公式复制为 LaTeX；
- 查看图表所在页和关联正文。

### 4.8 双语 Markdown 研究笔记导出

优先支持 Markdown，而不是先做复杂的 PDF 重排导出。

导出内容应保留：

- 原文和译文；
- 页码和结构块引用；
- 图片、表格和公式；
- 用户笔记、高亮和标签；
- 图片附件目录；
- 论文文件指纹和解析模型信息。

目标是兼容 Obsidian、Typora、Hana Note 等常用工具，并且导出后不丢失证据链。

## 0.7：研究工作台

目标：从单篇阅读扩展为可管理、可比较、可批量处理的研究工作流。

### 4.9 论文库

计划功能：

- 最近阅读；
- 收藏和标签；
- 按作者、年份和主题筛选；
- DOI、标题和作者元数据；
- 按文件哈希去重；
- 阅读进度和最近笔记；
- 解析缓存管理；
- 删除本地文件关联和缓存。

第一阶段做“研究阅读库”，不急于替代完整文献管理软件。

### 4.10 论文对比模式

两篇论文并排或按章节对齐，重点生成有证据的对照表，而不是泛泛判断哪篇更好。

建议对比维度：

| 维度 | 论文 A | 论文 B | 来源 |
| --- | --- | --- | --- |
| 研究问题 | ... | ... | p. x / p. y |
| 材料体系 | ... | ... | p. x / p. y |
| 实验条件 | ... | ... | p. x / p. y |
| 核心结果 | ... | ... | p. x / p. y |
| 局限性 | ... | ... | p. x / p. y |

### 4.11 面向化工科研的结构化提取

可以增加可选的“化工论文模式”，用于提取：

- 材料、单体和组成；
- 反应条件；
- 温度、时间和压力；
- 投料比和产率；
- 光照波长；
- 储热容量；
- 相变温度；
- 循环次数；
- 光热转换效率；
- 表征方法；
- 关键图表和证据页。

抽取结果必须带来源。例如对偶氮苯和丙烯酸酯共聚物论文，理想输出应能一键提取氢键作用、储能容量、光异构化条件、循环稳定性和关键表征结果，并且每项都能点回原文页码。

### 4.12 批量解析队列

计划功能：

- 一次导入多篇论文；
- 后台排队和进度显示；
- 限制并发，避免浪费 MinerU 配额；
- 失败任务单独重试；
- 完成后生成文献索引；
- 批量提取标题、摘要和关键词；
- 批量导出 Markdown 或结构化数据。

## 五、优先级矩阵

| 优先级 | 功能 | 原因 |
| --- | --- | --- |
| P0 | 段落级引用锚点 | 建立产品与普通阅读器的核心差异 |
| P0 | 论文内搜索 | 对长论文的收益立刻可见 |
| P0 | 自动目录 | 利用现有标题结构，成本低 |
| P0 | 锚定式笔记和书签 | 将阅读结果沉淀下来 |
| P0 | SHA-256 解析缓存 | 减少重复上传和等待 |
| P1 | 带证据的论文助手 | 把 Hana 助手和解析结构真正连接起来 |
| P1 | 术语表和翻译缓存 | 提升双语阅读的稳定性 |
| P1 | 图表、公式和图片实验室 | 直接服务文献汇报和实验分析 |
| P1 | Markdown 研究笔记导出 | 让结果离开当前卡片仍可复用 |
| P2 | 论文库 | 从单篇工具扩展为个人阅读库 |
| P2 | 论文对比 | 价值高，但需要更成熟的证据模型 |
| P2 | 化工结构化提取 | 适合形成领域特色 |
| P2 | 批量解析队列 | 需要任务、配额和缓存基础设施 |

## 六、暂时不做

### 6.1 不恢复本地 PDF 结构解析器

两套解析结果标准会增加维护成本，也会让用户无法判断不同模式的差异。PDF 结构解析保持 MinerU-only；PDF.js 只负责原页视觉参考和定位。

### 6.2 不急于引入复杂向量数据库

先用结构块、章节、关键词和当前选区完成可解释检索。只有当实际论文库规模和使用场景证明关键词检索不足时，再引入语义索引。

### 6.3 不先做完整 PDF 编辑器

涂鸦、页面重排、自由标注和 PDF 编辑会显著扩大项目边界，暂时不应抢占证据、搜索和笔记功能的资源。

### 6.4 不做无来源的自动总结

科研场景中，无法定位证据的漂亮总结不可靠。任何自动总结都应附带来源或明确标记为推断。

### 6.5 不把 Token 暴露给前端

Token 继续保存在服务端敏感配置中，不写入前端资源、浏览器本地存储、日志、测试夹具或发布包。

### 6.6 不在页面加载时自动发起高成本请求

MinerU 解析、批量翻译、论文问答和批量提取都应由用户明确触发，并提供取消、进度和失败状态。

## 七、建议的技术演进顺序

1. 为结构块建立稳定的论文指纹和引用 ID；
2. 在本地建立全文、译文和标题索引；
3. 增加定位、高亮、书签和锚定笔记；
4. 为 MinerU 结果增加可复用缓存和任务状态；
5. 将结构块上下文接入 Hana 助手；
6. 增加术语表、翻译缓存和图表实验室；
7. 增加 Markdown 研究笔记导出；
8. 在单篇流程稳定后再扩展论文库、对比和批量队列。

每一步都应保持以下边界：

- 结构解析仍只有 MinerU；
- 原页视觉参考仍由 PDF.js 负责；
- 外部请求仍由插件后端和受控白名单负责；
- Token 仍是服务端私密配置；
- 高成本操作仍由用户显式触发；
- 生成内容仍尽量保留页码和结构块证据。

## 八、成功标准

项目下一阶段是否成功，不以功能数量衡量，而以以下问题能否被稳定回答衡量：

- 我能否在长论文中快速找到一句话？
- 我能否知道这句话位于哪一页、哪一段？
- 我能否把助手的回答追溯回原文？
- 我能否把自己的笔记和判断绑定在原文上？
- 我能否在离开当前卡片后继续使用这些结果？
- 我能否减少重复解析、重复翻译和重复查找？

如果这些问题都能得到可靠答案，Hana Paper Reader 就不只是一个 PDF 阅读器，而是一个真正服务科研阅读的证据工作台。
