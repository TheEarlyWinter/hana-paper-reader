# Hana Paper Reader 产品路线图

> 当前稳定版本：`0.5.0`
> 主线选择：先把单篇论文的证据闭环做深，再扩展论文库与领域能力。
> 本文记录产品方向和版本顺序，不承诺具体发布日期。

## 一、战略结论

Hana Paper Reader 下一步**不应立刻冲向论文库、批量解析或向量 RAG**。

0.5.0 已经搭起搜索、大纲、笔记、书签、进度、任务、证据助手、术语、视觉实验室和 Markdown 导出等研究工具，但多数仍是第一版能力。当前最重要的不是继续增加入口，而是把单篇论文从导入到沉淀的完整流程做稳。

现阶段的主要风险包括：

- 单个 `paper-workspace.json` 承载全部研究数据；
- `assets/panel.js` 与 `routes/api.js` 已经开始膨胀；
- 缺少用户可见的数据与缓存管理；
- 证据助手只有“当前块 / 全文”的粗粒度范围；
- Markdown 已能导出，但还不是包含真实附件的完整研究包；
- 论文库、批量队列和跨论文分析都依赖更稳定的数据基础。

如果现在直接横向扩张，短期功能会迅速增多，长期却容易变成难以维护的全功能论文平台。下一阶段应优先修地基和闭环，而不是继续堆按钮。

## 二、产品定位

一句话定义：

> **Hana Paper Reader：从论文 PDF 到可复核研究结论的证据工作台。**

它的核心闭环是：

```text
导入论文
  → 快速定位
  → 向助手提问
  → 核验原文证据
  → 记录个人判断
  → 导出或再次使用
```

以后每个新功能都必须回答一个问题：

> 它是否让这条闭环更快、更可靠或更容易复用？

如果不能，就暂时不做。

## 三、路线选择

### 路线 A：快速扩张成全功能论文平台

可能包含：

- 论文库；
- Zotero 集成；
- 知识图谱；
- 向量 RAG；
- 多论文对比；
- 批量解析；
- 综述生成；
- 云同步。

**优点：** GitHub 页面看起来功能强大，产品覆盖面广。

**问题：** 项目边界会迅速失控，并直接进入 PaperQuay、Zotero 插件和成熟 AI 阅读器的主战场。

**结论：** 现在不走这条路线。

### 路线 B：把单篇论文证据闭环做深

重点包括：

- 更强的章节级检索；
- 助手回答严格区分原文、解释和推断；
- 点击引用直接定位 PDF bbox；
- 可编辑译文和研究笔记；
- 可检查模型实际使用了哪些证据；
- 完整、可迁移的研究包导出；
- 稳定的数据恢复和删除机制。

**优点：** 与 HanaAgent 的 Agent 能力结合最紧，产品差异最清楚，真实使用价值最高。

**问题：** 新功能列表没有路线 A 那么炫，但能建立更可靠的核心体验。

**结论：这是下一阶段的主线。**

### 路线 C：直接做化工科研垂直工具

重点提取：

- 材料与组成；
- 反应条件；
- 温度、压力和时间；
- 投料比与产率；
- 相变温度与储热容量；
- 光照波长与光热转换效率；
- 循环稳定性；
- 表征方法和关键图表。

**优点：** 非常贴合化工科研场景，容易形成独特价值。

**问题：** 当前直接开展会受到单篇阅读底座的限制，抽取结果也难以稳定绑定可靠证据。

**结论：** 在证据系统和论文库稳定后推进，作为中后期差异化方向。

## 四、版本演进顺序

```text
0.5.0  当前版本
  ↓
0.5.1  稳定性与数据所有权
  ↓
0.6.0  深度证据阅读
  ↓
0.6.1  可编辑翻译与研究笔记
  ↓
0.7.0  论文库
  ↓
0.8.0  多论文对比与化工结构化提取
  ↓
0.9.0  批量研究项目
  ↓
1.0.0  可信研究工作台
```

## 五、版本里程碑

### 0.5.0：可引用研究工作区

当前版本已经建立：

- MinerU-only PDF 结构解析；
- PDF.js 本地原页预览；
- 连续单栏双语阅读；
- `Page X / block Y` 引用锚点；
- 全文搜索与自动大纲；
- 锚定笔记、书签与阅读进度；
- SHA-256 解析缓存与任务状态；
- 后端核验的证据助手；
- 术语版本化翻译缓存；
- 图表、公式、图片与表格实验室；
- 双语 Markdown 导出；
- 最近论文及研究状态恢复。

0.5.0 的意义是证明产品闭环可行，后续版本负责把它做稳、做深并扩展。

### 0.5.1：稳定性与数据所有权

本版不追求炫技功能，重点补齐地基：

- 数据 schema 迁移与损坏恢复；
- 每篇论文独立存储，避免单一 JSON 无限膨胀；
- 论文数据、译文和缓存的管理与删除；
- 真实 PDF 回归样本；
- 可重复的打包与 GitHub Release 流程；
- 拆分过大的前后端模块；
- 明确更新、降级和卸载后的数据边界。

这一阶段的核心主题是：**用户必须知道数据在哪里、占了多少空间、如何备份、如何删除，以及版本升级后是否仍然安全。**

### 0.6.0：深度证据阅读

把证据助手真正做成核心能力：

- 当前段落、当前页面、当前章节、全文四种上下文范围；
- 展示实际注入模型的证据；
- 回答区分“论文原文”“助手解释”“助手推断”；
- 关键词相关度排序，而不只是字符串包含；
- 引用点击定位原文块和 PDF bbox；
- 章节总结、方法拆解和结论核验都保留来源。

目标不是让回答变得更长，而是让用户能够看见：**模型看了什么、结论来自哪里、哪些部分只是推断。**

### 0.6.1：可编辑翻译与研究笔记

让阅读结果真正属于用户：

- 人工修订译文；
- 区分 AI 译文与用户定稿；
- 术语别名、缩写和出现位置；
- 只重译受术语变化影响的段落；
- 笔记类型、标签、筛选和编辑；
- 从划词直接创建笔记；
- 导出 Markdown 与图片附件组成的完整 ZIP 研究包。

这一阶段的重点是从“模型生成结果”转向“用户可以修订、确认并长期保存的研究资料”。

### 0.7.0：论文库

单篇流程稳定后，再扩展为个人研究阅读库：

- 最近阅读；
- 收藏和标签；
- 标题、作者、年份与 DOI；
- 阅读进度和最近笔记；
- 按文件哈希去重；
- 存储空间和缓存管理；
- 删除论文及其关联研究数据；
- 工作区导入、导出和备份。

第一阶段只做“研究阅读库”，不急于替代 Zotero 或完整文献管理软件。

### 0.8.0：多论文对比与化工结构化提取

开始形成 Hana Paper Reader 的领域特色：

- 两篇或多篇论文的证据对照表；
- 对研究问题、方法、实验条件、结果和局限进行比较；
- 每个对比单元格都能回到真实来源；
- 提供化工、材料、储能和光热等领域提取模板；
- 支持 CSV、Markdown 和结构化 JSON 导出；
- 缺失数据明确标记为“未报告”，禁止模型猜测补全。

这一版本的关键不是自动生成漂亮表格，而是让表格里的每个数值和判断都能被核验。

### 0.9.0：批量研究项目

在任务、缓存和论文库成熟后扩展批量工作流：

- 多篇论文导入队列；
- 并发与 MinerU 配额控制；
- 失败任务单独重试；
- 项目文件夹；
- 跨论文提问；
- 批量结构化提取和汇总；
- 只有真实使用证明关键词检索不足时，才考虑本地向量索引。

批量能力必须建立在清晰的任务状态、成本控制和失败恢复之上，不能只是循环调用单篇接口。

### 1.0.0：可信研究工作台

1.0 不以功能数量决定，而以可靠性决定：

- 数据升级不丢失；
- 引用不能指向不存在的结构块；
- 重开、更新和异常退出后可以恢复；
- 导出结果能够重新导入；
- 多种真实论文可以稳定解析和阅读；
- 安装、更新、卸载和清理流程完整；
- 隐私与外发边界清楚；
- 用户能够明确知道哪些内容来自论文，哪些来自模型。

达到这些条件后，Hana Paper Reader 才称得上一个可信的研究工作台。

## 六、近期明确不做

### 不恢复本地 PDF 结构解析器

PDF 结构解析继续保持 MinerU-only；PDF.js 只负责本地原页视觉参考和定位。两套解析结果标准会增加维护成本，也会让用户难以判断不同模式的差异。

### 不做完整 PDF 编辑器

涂鸦、页面重排、自由标注和 PDF 编辑会显著扩大边界，暂时不应抢占证据、搜索和笔记功能的资源。

### 不做无来源的自动总结

科研场景中，无法定位证据的漂亮总结并不可靠。任何自动总结都必须附带来源，或明确标记为助手推断。

### 不把 Token 暴露给前端

Token 继续保存在 Hana 的服务端敏感配置中，不写入 WebView 资源、浏览器存储、日志、测试夹具或发布包。

### 不在页面加载时自动调用高成本服务

MinerU 解析、批量翻译、论文问答和批量提取都必须由用户明确触发，并提供进度、取消和失败状态。

### 不为了“AI 感”过早引入向量数据库

先使用结构块、章节、关键词和当前选区完成可解释检索。只有当真实论文库规模与使用场景证明关键词检索不足时，再评估本地向量索引。

### 不急着重写为 React 或 npm 项目

当前 direct WebView 结构仍然适合项目规模。只有当模块复杂度、复用需求和测试成本证明迁移收益足够高时，才考虑改变技术栈。

### 不先做账号、云同步和多人协作

这些功能会引入全新的身份、权限、冲突和隐私问题，不属于近期主线。

### 不把功能广度当成成熟度

新增入口和按钮不是版本成功的证明。可靠的证据链、数据恢复和可迁移结果优先级更高。

## 七、决策原则

后续需求进入版本计划前，至少满足以下一项：

- 明显缩短导入、定位、核验、记录或导出的路径；
- 提高引用、数据或模型回答的可靠性；
- 让研究结果更容易迁移、备份或再次使用；
- 解决已经在真实使用中出现的稳定性问题；
- 为后续论文库、对比或批量能力提供不可缺少的基础设施。

如果一项功能只增加视觉复杂度或营销描述，却不能改善证据闭环，就不进入近期版本。

## 八、总体成功标准

项目下一阶段是否成功，不以功能数量衡量，而以这些问题能否被稳定回答衡量：

- 我能否在长论文中快速找到一句话？
- 我能否知道这句话位于哪一页、哪一个结构块？
- 我能否把助手的回答追溯回原文？
- 我能否看到模型实际使用了哪些证据？
- 我能否区分论文原文、助手解释和助手推断？
- 我能否把自己的笔记、译文和判断绑定到原文？
- 我能否在离开当前卡片后继续使用这些结果？
- 我能否安全地备份、迁移和删除研究数据？
- 我能否减少重复解析、重复翻译和重复查找？

当这些问题都能得到可靠答案时，Hana Paper Reader 就不只是一个 PDF 阅读器，而是一个真正服务科研阅读的证据工作台。
