# Hana Paper Reader 产品路线图

> 当前源码版本：`0.8.0`
> 最新公开稳定版本：`0.7.1`
> 主线选择：先把单篇论文的证据闭环做深，再扩展论文库与领域能力。
> 本文记录产品方向和版本顺序，不承诺具体发布日期。

## 一、战略结论

Hana Paper Reader 下一步**不应立刻冲向论文库、批量解析或向量 RAG**。

0.6.0 已将搜索、大纲、笔记、书签、进度、证据助手、术语、视觉块和 Markdown 导出接入统一 Evidence 证据对象，并重组为“定位、核验、沉淀”三条工作流。0.6.1 又补齐四类证据型笔记、页/章节/全文可解释检索、图表证据区、分篇存储、数据分项清理、备份恢复、精确 UI 状态恢复和固定发版回归。0.6.2 新增逐段上下排列的「对照」阅读视图，同时保留原有三种阅读模式。0.6.3 修复划词发送的会话路由边界，要求目标明确且禁止静默创建会话。0.7.0 在不修改 Hana 全局 Agent 配置的前提下，为 Paper Reader 增加动态 Agent 与聊天模型选择，以及模型绑定的内部会话隔离。0.8.0 解决 Hana WebView 导出不可靠落盘和多实例工作区写入竞争，优先由插件后端写入 Windows `Downloads`，并保留兼容回退。

现阶段的主要风险包括：

- `assets/panel.js`、`assets/research-tools.js` 与 `routes/api.js` 仍需继续按职责拆分；
- Markdown 已能导出，图表也可导出 CSV / LaTeX，但带真实附件的可迁移研究包仍待完善；
- 固定合成夹具已经建立，后续版本仍需持续补充真实世界 PDF 类型；
- 论文库、批量队列和跨论文分析必须建立在 0.6.1 分篇存储和删除边界之上。

如果现在直接横向扩张，短期功能会迅速增多，长期却容易变成难以维护的全功能论文平台。下一阶段应优先修地基和闭环，而不是继续堆按钮。

## 二、产品定位

一句话定义：

> **Hana Paper Reader：从论文 PDF 到可复核研究结论的证据工作台。**

它的核心闭环是：

```text
导入论文
  → 快速定位
  → 向助手提问
  → 核验原文证据
  → 记录个人判断
  → 导出或再次使用
```

以后每个新功能都必须回答一个问题：

> 它是否让这条闭环更快、更可靠或更容易复用？

如果不能，就暂时不做。

## 三、路线选择

### 路线 A：快速扩张成全功能论文平台

可能包含：

- 论文库；
- Zotero 集成；
- 知识图谱；
- 向量 RAG；
- 多论文对比；
- 批量解析；
- 综述生成；
- 云同步。

**优点：** GitHub 页面看起来功能强大，产品覆盖面广。

**问题：** 项目边界会迅速失控，并直接进入 PaperQuay、Zotero 插件和成熟 AI 阅读器的主战场。

**结论：** 现在不走这条路线。

### 路线 B：把单篇论文证据闭环做深

重点包括：

- 更强的章节级检索；
- 助手回答严格区分原文、解释和推断；
- 点击引用直接定位 PDF bbox；
- 可编辑译文和研究笔记；
- 可检查模型实际使用了哪些证据；
- 完整、可迁移的研究包导出；
- 稳定的数据恢复和删除机制。

**优点：** 与 HanaAgent 的 Agent 能力结合最紧，产品差异最清楚，真实使用价值最高。

**问题：** 新功能列表没有路线 A 那么炫，但能建立更可靠的核心体验。

**结论：这是下一阶段的主线。**

### 路线 C：直接做化工科研垂直工具

重点提取：

- 材料与组成；
- 反应条件；
- 温度、压力和时间；
- 投料比与产率；
- 相变温度与储热容量；
- 光照波长与光热转换效率；
- 循环稳定性；
- 表征方法和关键图表。

**优点：** 非常贴合化工科研场景，容易形成独特价值。

**问题：** 当前直接开展会受到单篇阅读底座的限制，抽取结果也难以稳定绑定可靠证据。

**结论：** 在证据系统和论文库稳定后推进，作为中后期差异化方向。

## 四、版本演进顺序

```text
0.5.0  已公开：可引用研究工作区
  ↓
0.6.0  已完成：统一 Evidence 与轻量研究工作流
  ↓
0.6.1  已公开：稳定性与数据所有权
  ↓
0.6.2  已公开：逐段上下对照阅读
  ↓
0.6.3  已完成：显式会话路由与手动目标选择
  ↓
0.7.0  已完成：动态 Agent / 聊天模型选择与会话隔离
  ↓
0.8.0  已完成：WebView 可靠导出与研究状态稳定性
  ↓
0.9.0  论文库与可迁移研究包
  ↓
0.10.0  多论文对比与化工结构化提取
  ↓
0.11.0  批量研究项目
  ↓
1.0.0  可信研究工作台
```

## 五、版本里程碑

### 0.5.0：可引用研究工作区

当前版本已经建立：

- MinerU-only PDF 结构解析；
- PDF.js 本地原页预览；
- 连续单栏双语阅读；
- `Page X / block Y` 引用锚点；
- 全文搜索与自动大纲；
- 锚定笔记、书签与阅读进度；
- SHA-256 解析缓存与任务状态；
- 后端核验的证据助手；
- 术语版本化翻译缓存；
- 图表、公式、图片与表格实验室；
- 双语 Markdown 导出；
- 最近论文及研究状态恢复。

0.5.0 的意义是证明产品闭环可行，后续版本负责把它做稳、做深并扩展。

### 0.6.0：统一 Evidence 与轻量研究工作流

已经建立：

- 由 `paperHash + blockId` 确定性生成的稳定 Evidence ID；
- 原文、译文、章节、页码、bbox 和视觉资源共用的 Evidence 契约；
- 搜索、证据助手、划词问答、会话发送、笔记、书签与视觉块统一消费 Evidence；
- “定位、核验、沉淀”三条一级研究工作流；
- 原文、双语、译文三种轻量阅读模式；
- AI 译文与用户定稿，用户定稿默认锁定且不会被全文翻译或术语变化覆盖；
- 工作区 schema 2、旧数据迁移备份和 Evidence 关系修复；
- 带 Evidence ID、AI 译文/用户定稿标记的 Markdown 导出。

本版目标不是增加更多研究入口，而是让现有功能共享同一份可定位、可核验、可沉淀的证据。

### 0.6.1：稳定性与数据所有权

已公开版本已经建立：

- 工作区 schema 3 和 `per-paper-v1` 分篇目录，旧数据迁移前自动备份；
- 论文结构、视觉缓存、译文、笔记与书签的占用统计和用户可见管理；
- 解析视觉缓存、AI 译文、论文结构、整篇研究数据四种边界明确的清理动作；
- 完整 JSON 备份与恢复，恢复前校验附件路径、Base64、数量、单附件与总大小；
- 研究发现、方法与条件、疑问、局限与风险四类证据型笔记，支持标签、筛选、编辑、解决状态和划词创建；
- 当前页、当前章节、全文范围，以及原文、译文、双语和块类型筛选；
- 命中高亮、上下文和基于规则的可解释搜索评分，不引入向量数据库；
- 图表证据区，以及带来源 Markdown、表格 CSV 和公式 LaTeX；
- 活动段落、双侧滚动、阅读模式、笔记草稿和搜索条件精确恢复；
- 英文双栏、中文、扫描页、公式密集、图表/表格密集五类固定合成 PDF 回归样本；
- 语法检查、测试、ZIP、反解、复测、敏感扫描、SHA-256 和 UTF-8 Release Notes 发版门禁。

这一阶段的核心主题是：**用户必须知道数据在哪里、占了多少空间、如何备份、如何删除，并能把每条研究判断带回论文证据。**

### 0.6.2：逐段上下对照阅读

- 新增「对照」模式，按段落把英文原文与中文译文上下紧邻排列；
- 保留原文、双语和译文三种既有模式；
- 视觉本体只展示一次，翻译、定稿、引用、搜索、笔记与定位继续复用统一块状态；
- 单独保存并恢复对照模式滚动位置。

### 0.6.3：显式会话路由与手动目标选择

这一版先修复会话发送的语义错误，不把插件卡片的表面身份误当成 Hana 当前主聊天：

- 划词发送必须先选择公开目标对话；
- 目标令牌短期有效，前端不接触宿主 session path，发送前重新校验；
- 「新建对话并发送」作为独立动作，只有用户明确点击时才调用 `session:create`；
- Card Center 独立打开时不猜测当前主聊天；宿主尚未提供可信来源 `sessionRef` 前，保留手动选择作为唯一目标路由；
- 用回归测试锁定“无目标不创建、已有目标发往指定会话、路径型 session projection 兼容”。

原生子对话仍是后续临时论文解释的目标方案，待 Hana 开放正式插件接口后再接入。

### 0.7.0：动态 Agent / 聊天模型选择与会话隔离

本版先把模型选择边界固定下来，再扩展论文库：

- 动态发现本机全部 Agent；
- 通过 `provider:models-by-type({ type: "chat" })` 动态发现已配置聊天模型；
- 每个 Agent 独立保存「跟随 Agent」或显式 `provider/id` 偏好；
- 翻译、划词问答、证据问答和显式新建会话使用所选模型；
- 模型无效或不可用时后端显式拒绝，不静默回退；
- 已有普通会话只发送内容，不改写其绑定模型。

### 0.8.0：WebView 可靠导出与研究状态稳定性

本版不扩张为论文库，而是先把研究成果从 Hana 卡片可靠交付到用户文件系统，并修复快速操作和多实例场景下的数据边界：

- 双语 Markdown 与完整 JSON 备份优先由插件后端直接写入 Windows `Downloads` 目录；
- 非法 Windows 文件名清洗和同名文件序号化，避免覆盖已有成果；
- 宿主 `resource.open` 与原生导航下载回退，兼容旧版 Hana WebView；
- 下载友好的 GET 导出路由、附件响应头、缓存控制和成功/失败可见提示；
- 多个插件路由实例对同一工作区的写入串行化，避免陈旧快照覆盖术语、笔记或论文状态；
- 术语版本、AI 译文缓存、用户定稿和备份恢复边界的回归验证。

这一版的判断标准是：研究成果必须真的落盘，且不能因为快速切换论文或同时写入而丢失。

### 0.9.0：论文库与可迁移研究包

单篇流程稳定后，再扩展为个人研究阅读库，并补齐跨设备研究成果迁移：

- 最近阅读；
- 收藏和标签；
- 标题、作者、年份与 DOI；
- 阅读进度和最近笔记；
- 按文件哈希去重；
- 跨论文的总存储空间与缓存汇总；
- 论文库层面的删除、归档和批量备份；
- Markdown、Evidence JSON 与真实图片附件组成的可迁移研究包。

第一阶段只做“研究阅读库”，不急于替代 Zotero 或完整文献管理软件。

### 0.10.0：多论文对比与化工结构化提取

开始形成 Hana Paper Reader 的领域特色：

- 两篇或多篇论文的证据对照表；
- 对研究问题、方法、实验条件、结果和局限进行比较；
- 每个对比单元格都能回到真实来源；
- 提供化工、材料、储能和光热等领域提取模板；
- 支持 CSV、Markdown 和结构化 JSON 导出；
- 缺失数据明确标记为“未报告”，禁止模型猜测补全。

这一版本的关键不是自动生成漂亮表格，而是让表格里的每个数值和判断都能被核验。

### 0.11.0：批量研究项目

在任务、缓存和论文库成熟后扩展批量工作流：

- 多篇论文导入队列；
- 并发与 MinerU 配额控制；
- 失败任务单独重试；
- 项目文件夹；
- 跨论文提问；
- 批量结构化提取和汇总；
- 只有真实使用证明关键词检索不足时，才考虑本地向量索引。

批量能力必须建立在清晰的任务状态、成本控制和失败恢复之上，不能只是循环调用单篇接口。

### 1.0.0：可信研究工作台

1.0 不以功能数量决定，而以可靠性决定：

- 数据升级不丢失；
- 引用不能指向不存在的结构块；
- 重开、更新和异常退出后可以恢复；
- 导出结果能够重新导入；
- 多种真实论文可以稳定解析和阅读；
- 安装、更新、卸载和清理流程完整；
- 隐私与外发边界清楚；
- 用户能够明确知道哪些内容来自论文，哪些来自模型。

达到这些条件后，Hana Paper Reader 才称得上一个可信的研究工作台。

## 六、近期明确不做

### 不恢复本地 PDF 结构解析器

PDF 结构解析继续保持 MinerU-only；PDF.js 只负责本地原页视觉参考和定位。两套解析结果标准会增加维护成本，也会让用户难以判断不同模式的差异。

### 不做完整 PDF 编辑器

涂鸦、页面重排、自由标注和 PDF 编辑会显著扩大边界，暂时不应抢占证据、搜索和笔记功能的资源。

### 不做无来源的自动总结

科研场景中，无法定位证据的漂亮总结并不可靠。任何自动总结都必须附带来源，或明确标记为助手推断。

### 不把 Token 暴露给前端

Token 继续保存在 Hana 的服务端敏感配置中，不写入 WebView 资源、浏览器存储、日志、测试夹具或发布包。

### 不在页面加载时自动调用高成本服务

MinerU 解析、批量翻译、论文问答和批量提取都必须由用户明确触发，并提供进度、取消和失败状态。

### 不为了“AI 感”过早引入向量数据库

先使用结构块、章节、关键词和当前选区完成可解释检索。只有当真实论文库规模与使用场景证明关键词检索不足时，再评估本地向量索引。

### 不急着重写为 React 或 npm 项目

当前 direct WebView 结构仍然适合项目规模。只有当模块复杂度、复用需求和测试成本证明迁移收益足够高时，才考虑改变技术栈。

### 不先做账号、云同步和多人协作

这些功能会引入全新的身份、权限、冲突和隐私问题，不属于近期主线。

### 不把功能广度当成成熟度

新增入口和按钮不是版本成功的证明。可靠的证据链、数据恢复和可迁移结果优先级更高。

## 七、决策原则

后续需求进入版本计划前，至少满足以下一项：

- 明显缩短导入、定位、核验、记录或导出的路径；
- 提高引用、数据或模型回答的可靠性；
- 让研究结果更容易迁移、备份或再次使用；
- 解决已经在真实使用中出现的稳定性问题；
- 为后续论文库、对比或批量能力提供不可缺少的基础设施。

如果一项功能只增加视觉复杂度或营销描述，却不能改善证据闭环，就不进入近期版本。

## 八、总体成功标准

项目下一阶段是否成功，不以功能数量衡量，而以这些问题能否被稳定回答衡量：

- 我能否在长论文中快速找到一句话？
- 我能否知道这句话位于哪一页、哪一个结构块？
- 我能否把助手的回答追溯回原文？
- 我能否看到模型实际使用了哪些证据？
- 我能否区分论文原文、助手解释和助手推断？
- 我能否把自己的笔记、译文和判断绑定到原文？
- 我能否在离开当前卡片后继续使用这些结果？
- 我能否安全地备份、迁移和删除研究数据？
- 我能否减少重复解析、重复翻译和重复查找？

当这些问题都能得到可靠答案时，Hana Paper Reader 就不只是一个 PDF 阅读器，而是一个真正服务科研阅读的证据工作台。
