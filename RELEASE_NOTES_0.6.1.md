# Hana Paper Reader 0.6.1

0.6.1 完成了单篇论文证据闭环的稳定性与数据所有权建设。本版没有扩展论文库或多论文对比，而是把研究笔记、论文内检索、图表证据、恢复体验、数据管理和发布工程做完整。

## 证据型研究笔记

- 笔记固定为四类：研究发现、方法与条件、疑问、局限与风险。
- 从原文或译文划选后创建笔记，自动绑定原文摘录、页码、结构块、当前译文和稳定 Evidence ID。
- 支持按章节、类型、标签和未解决疑问筛选。
- 支持编辑、删除、疑问解决状态，以及整理为带来源的 Markdown 阅读笔记。
- 即使用户选择删除论文结构，笔记仍保留 Evidence 快照，不会脱离来源。

## 搜索与大纲联动

- 搜索范围：当前页面、当前章节、全文。
- 搜索语种：原文、译文、原文与译文。
- 块类型筛选：标题、正文、图题/图片、表格、公式。
- 命中词在结果和正文中高亮；点击后同步定位原文、译文和 PDF 原页。
- 结果按词频、标题权重、视觉类型、当前页与邻近块排序，并展示评分理由。
- 大纲显示阅读进度、书签数和未解决疑问，并在章节旁显示关联状态。
- 未引入向量数据库，排序保持本地、透明、可解释。

## 图表证据区

- 每个图、表、公式同时展示本体、图题/表题、章节、页码、前后相关正文和对应译文。
- 支持一键回到正文和 PDF 原页。
- 支持复制带论文、页码、block 与 Evidence ID 的 Markdown。
- 表格可导出 UTF-8 CSV，公式可复制 LaTeX。

## 数据所有权

- 工作区 schema 升级到 3；每篇论文使用独立目录保存结构、研究记录、译文和任务，不再把全部内容塞进一个 JSON。
- 旧 schema 首次加载前自动备份，再迁移到 `per-paper-v1` 布局。
- 数据页展示论文结构、图片与表格缓存、译文缓存、笔记与书签和总占用空间。
- 明确分离四种删除动作：
  - 仅清理解析视觉缓存；
  - 仅清理 AI 译文，保留用户定稿；
  - 保留证据型笔记后删除论文结构；
  - 删除整篇论文及其全部研究数据。
- 每个动作都在确认框中写明删除和保留范围。
- 支持完整 JSON 备份与恢复；恢复前校验格式、路径、Base64、附件数量、单附件及总大小。
- 备份不包含 MinerU Token 或模型会话。

## 新手与恢复体验

- 首屏只保留“体验示例论文 / 配置 MinerU / 导入我的论文”三个主要动作。
- 解析状态改为直白中文：正在上传论文、正在等待 MinerU 解析、正在整理正文和图表、论文已准备好。
- 恢复上次活动段落、原文与译文滚动位置、阅读模式、未提交笔记草稿、搜索关键词和筛选条件。
- 重新打开后明确提示：研究内容已恢复；重新选择同一 PDF 可恢复原页预览，不会重复解析。
- “MinerU 重解析”现在会明确确认并使用 `force=1` 绕过解析结构缓存。

## 发布工程与固定回归

- 新增 5 份无私人数据的固定合成 PDF：英文双栏、中文、扫描页、公式密集、图表/表格密集。
- OCR fallback 继续由协议夹具验证“普通模式失败 → OCR 成功”。
- 发布脚本执行：语法检查 → 测试 → 构建 ZIP → 反向解包 → 解包后再次测试 → 敏感数据扫描 → SHA-256 → QA 报告。
- GitHub Actions 直接从 UTF-8 `RELEASE_NOTES_0.6.1.md` 发布 Release Notes，不再通过 PowerShell 双引号 Here-String 拼接 Markdown。

## 保留兼容能力

- MinerU 原始 `application/pdf` 二进制上传；
- 0.4.0 旧卡片 Base64 JSON 接收兼容；
- OCR fallback；
- PDF.js 本地原页预览；
- 无 npm 依赖、无构建步骤的 direct WebView 架构；
- 0.6.0 稳定 Evidence ID、三条工作流、三种阅读模式和用户定稿防覆盖。
