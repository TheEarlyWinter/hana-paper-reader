# Hana Paper Reader 0.9.0

0.9.0 是论文库核心交互与 WebView 兼容性修复版。它把删除确认、视觉对象导出、剪贴板降级和公式 LaTeX 复制从“代码路径存在”推进到可在 HanaAgent 实机中操作和观察的完整路径。

## 文库交互

- 文库删除按钮使用稳定容器事件委托，重绘列表后仍能响应。
- 删除确认弹窗移到 `document.body`，避免嵌入式布局溢出裁剪和点击层级问题。
- 取消确认不会调用删除接口；确认操作只执行一次，并保留明确的成功/失败提示。
- 论文库继续保持按论文 hash 隔离，收藏、归档、筛选、排序和元数据状态不因本轮修复被改变。

## 图表、表格与公式

- 新增 `/api/research/csv` GET/POST 导出接口，返回附件响应头并支持直接保存到 Windows `Downloads`。
- CSV 下载保留直接落盘、宿主 `resource.open`、Blob 和原生导航四级路径；任一路径失败时不会静默结束。
- CSV 对引号、逗号、换行和以公式运算符开头的单元格进行处理，降低表格软件公式注入风险。
- 图表/表格/公式工具操作按钮增加可见文案、`title`、`aria-label` 和操作标识。
- 公式块有 `latex` 或仅有可用 `text` 时均显示“复制 LaTeX”入口。

## 剪贴板与 WebView 兼容

- 复制顺序为宿主剪贴板、浏览器 Clipboard API、`textarea + execCommand("copy")` 降级。
- 浏览器拒绝剪贴板写入时，界面显示明确失败提示，不再报告无反馈。
- 修复资源基地址清洗误删 `pluginSurfaceSession` 的问题；同源资源会保留宿主会话凭据，跨源脚本资源仍被拒绝。
- 保留 Blob 下载和原生导航回退，兼容缺少部分宿主能力的旧 WebView。

## 验证

- Windows 10.0.19045 / HanaAgent 0.769.0 / Hana Paper Reader 0.9.0 开发版实机回归：删除取消、CSV、带来源 Markdown、LaTeX 复制和资源加载均已由用户确认无问题。
- Node 全量测试：75/75 通过。
- 发布脚本继续执行 JavaScript/MJS 语法检查、净目录打包、反向解包逐文件哈希比对、解包复测、敏感材料扫描和 SHA-256。

> 0.9.0 不修改 HanaAgent 宿主源码，不把 Token、模型凭据或私人论文写入发布包。
