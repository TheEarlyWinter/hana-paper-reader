# Hana Paper Reader 0.7.0

0.7.0 为 Paper Reader 增加了可验证的 Agent / 聊天模型选择能力，同时保留现有会话路由边界。

## 动态 Agent 与模型选择

- Agent 列表从宿主 `agent:list` 与本机 Agent 配置动态发现，不再限制为固定助手名单。
- 新增 `/api/models`，通过稳定的 `provider:models-by-type({ type: "chat" })` 读取已配置聊天模型。
- 每个 Agent 都可单独选择「跟随 Agent」或显式 `provider/id` 模型，偏好按 Agent 保存在浏览器本地。
- 后端对每次显式模型请求重新核验目录；模型格式无效、目录不可用或模型已下线时返回明确错误，不静默回退。

## 模型作用范围

- 选定模型会用于 Paper Reader 创建的翻译、划词问答、证据问答和「新建对话并发送」会话。
- 不同模型使用不同的 Paper Reader 内部会话缓存与并发锁，切换模型不会复用另一模型的会话。
- 「发送到所选已有对话」仍只调用 `session:send`，不会修改该普通会话绑定的 Agent 或模型；请求中的模型选择也不会被用于已有会话。

## 交互修复

- 修复 Agent / 模型下拉菜单在原生模型选择器上按住鼠标再松开时被误判为外部点击、菜单立即消失的问题。
- 点击模型下拉菜单内部不会再触发外部关闭逻辑；点击选择器区域外仍会正常关闭菜单。

## 兼容性与验证

- 最低 Hana 版本提升为 `0.686.15`，用于支持 `session:create` 的模型覆盖参数。
- 增加模型发现、精确模型校验、会话创建 payload、缓存隔离和已有会话不切换模型的回归覆盖。
- 继续保留 MinerU 二进制上传、PDF 原页预览、Evidence 引用和四种阅读模式。
