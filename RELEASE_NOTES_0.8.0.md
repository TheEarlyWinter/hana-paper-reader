# Hana Paper Reader 0.8.0

0.8.0 将研究导出从“浏览器发起下载”升级为“插件后端可靠落盘”，并继续收紧论文工作区、术语状态和备份恢复的边界。重点是让研究成果在 Hana WebView 中真正写入用户文件系统，同时保留旧宿主环境的兼容路径。

## 研究导出可靠落盘

- 双语 Markdown 和完整研究备份现在优先由插件后端 Node.js 直接写入 Windows `Downloads` 目录。
- 导出成功后返回实际文件名、路径和字节数，界面显示明确的保存提示，并尝试在文件管理器中定位产物。
- 论文标题会经过非法 Windows 文件名字符清洗；同名文件自动追加 `(1)`、`(2)` 等后缀，不覆盖已有研究成果。
- 保留宿主 `resource.open` 和原生导航下载作为兼容回退，不依赖 Blob URL 在 Hana WebView 中一定触发落盘。
- Markdown 与完整备份接口增加下载友好的 GET 路径、`Content-Disposition`、`Cache-Control: private, no-store` 和论文 hash 响应头。
- 研究工具通知改为短时面板提示，导出成功、失败和回退路径都有可见反馈。

## 工作区与术语稳定性

- 多个插件路由或 WebView 实例同时写入同一篇论文时，工作区变更通过进程内队列串行化，避免旧实例覆盖较新的术语、笔记或论文状态。
- 术语更新会即时刷新当前论文的术语版本和翻译缓存边界；用户定稿继续保留，受影响的 AI 译文明确标记为待重译。
- 术语请求增加论文版本和请求序列校验，切换论文或快速连续操作时不会把旧响应写入当前阅读器。
- 研究 API 返回的论文视图包含当前术语版本和术语内容，备份恢复后可以继续使用同一研究状态。

## 备份与兼容性

- 完整 JSON 备份继续包含论文结构、Evidence、翻译状态、笔记、书签、进度、术语、任务和可选视觉附件。
- 恢复前仍执行附件路径、Base64、数量、单附件大小和总大小校验；备份不包含 MinerU Token、宿主 session path 或模型会话凭据。
- 保持 Hana 最低版本 `0.686.15`、MinerU 原始二进制上传、OCR fallback、PDF.js 原页预览以及 0.7.x 的动态 Agent / 聊天模型选择能力。

## 验证

发布门禁包含：

- 全部 JavaScript / MJS 语法检查；
- 源码目录测试；
- 净目录 ZIP 打包；
- 反向解包逐文件 SHA-256 比对；
- 解包目录再次语法检查和测试；
- 敏感信息扫描；
- ZIP SHA-256 与 UTF-8 发布说明校验。

> 0.8.0 不修改 HanaAgent 宿主源码，也不需要 `allow-downloads` 补丁。
